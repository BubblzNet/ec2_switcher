require 'spec_helper'

describe Ec2Switcher do
  it 'has a version number' do
    expect(Ec2Switcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
